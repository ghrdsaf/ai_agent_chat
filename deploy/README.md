# deploy

部署目录占位。

## 后续建议包含

- `docker/`：本地开发和单机部署。
- `k8s/`：后续规模化部署。
- `nginx/`：反向代理和 HTTPS 配置。
- `database/`：数据库初始化和迁移说明。

## 第一阶段部署重点

MVP 阶段优先保证：

1. Spring Boot 后端可稳定访问。
2. Python AI 服务可被后端调用。
3. Chrome 插件能配置后端 API 地址。
4. 数据库能保存平台账号、会话、消息、建议和操作记录。

暂时不需要复杂的 K8s 部署。
